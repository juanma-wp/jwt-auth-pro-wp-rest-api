default:
  suites:
    default:
      paths:
        - '%paths.base%/tests/behat/features'
      contexts:
        - FeatureContext

  formatters:
    pretty:
      verbose: true
      paths: false
      snippets: false
